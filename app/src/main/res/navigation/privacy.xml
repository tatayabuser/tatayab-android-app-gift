<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/privacy"
    app:startDestination="@id/destination_privacy">

    <fragment
        android:id="@+id/destination_privacy"
        android:name="com.tatayab.tatayab.privacy.PrivacyFragment"
        android:label="@string/fragment_privacy_title"
        tools:layout="@layout/fragment_privacy">

        <argument
            android:name="url"
            app:argType="string" />

        <argument
            android:name="title"
            app:argType="string" />


    </fragment>

</navigation>
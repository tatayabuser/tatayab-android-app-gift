<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/order_tracking"
    app:startDestination="@id/destination_order_tracking">

    <fragment
        android:id="@+id/destination_order_tracking"
        android:name="com.tatayab.tatayab.ordertracking.OrderTrackingFragment"
        android:label="@string/fragment_order_tracking_title"
        tools:layout="@layout/fragment_order_tracking">

            <argument
                android:name="orderId"
                app:argType="string"
                app:nullable="true" />

        <argument
            android:name="orderDate"
            app:argType="string"
            app:nullable="true" />
    </fragment>


</navigation>